server:
  port: 80

spring:
  h2:
    console:
      path: /h2
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:hello-querydsl;
    username: abc
    password: abc

  jpa:
    show-sql: true
